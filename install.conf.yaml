- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.emacs: emacs/emacs
    ~/.gitconfig: git/config
    ~/.tmux.conf: tmux.conf
    ~/.zshrc: zsh/zshrc

- shell:
  - [git submodule update --init --recursive, Installing submodules]
