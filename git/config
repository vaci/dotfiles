[user]
	email = vaci@vaci.org
	name = Vaci
[alias]
        last = log -1 --stat
      	m = checkout master
        merged = branch --merged
	st = status --short --branch
	co = checkout
[color]
        ui = auto
